<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body>
  <% include partials/_header %>
  <ul>
    <% for (let url of Object.keys(urls)) { %>
      <li><%= url %> - <%= urls[url] %>
    <% } %>
  </ul>
</body>
</html>
